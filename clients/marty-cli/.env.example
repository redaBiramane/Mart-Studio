# Base de l'API Marty (sans slash final). Note le www (le domaine nu redirige).
MARTY_API_URL=https://www.martstudio.it.com

# Clé d'API (doit figurer dans MARTY_API_KEYS côté serveur Marty)
MARTY_API_KEY=chgt-moi
